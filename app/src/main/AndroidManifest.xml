<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="santriprogrammer.com.bookinglapangan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jatiraden"
        android:label="@string/app_name"
        android:roundIcon="@drawable/jatiraden"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".order.EditLapanganActivity"></activity>
        <activity android:name=".news.addnews.AddNewsActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".announcement.AnnouncementActivity" />
        <activity android:name=".news.detail.DetailNewsActivity" />
        <activity android:name=".news.NewsActivity" />
        <activity android:name=".buktitransfer.UploadBuktiTransferActivity" />
        <activity android:name=".UploadLapanganActivity" />
        <activity android:name=".books.BookingActivity" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>